
  # Car game with birthday surprise

  This is a code bundle for Car game with birthday surprise. The original project is available at https://www.figma.com/design/nUzoFlyVG4iOV4CctJp8Mc/Car-game-with-birthday-surprise.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  